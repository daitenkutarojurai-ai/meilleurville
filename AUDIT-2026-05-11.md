# Audit 2026-05-11 — meilleurville

Weekly autonomous audit. Run on commit `9b7cc26` (post-pull `main`). Prior
weekly audit one day before (`AUDIT-2026-05-10.md`) — most of the heavy
mechanical fixes were applied then; this run picks up two of last week's
TODO items.

## Auto-fixed (this run)

- [2629d10] `audit: add metadataBase + homepage canonical to root metadata` —
  added `metadataBase: new URL(SITE_URL)` + `alternates: { canonical: "/" }`
  in `app/layout.tsx`. Resolves all relative metadata URLs (OG images,
  Twitter card) to absolute paths automatically. Last week flagged the
  missing canonical as a top SEO TODO.
- [9b7cc26] `audit: add aria-label to unlabeled search/email inputs` —
  4 user-facing inputs in `components/HeroSection.tsx`, `VillesSearch.tsx`,
  `LeaderboardTable.tsx`, `NewsletterSection.tsx` had only a `placeholder`
  (lost on focus for screen readers). Each now has an explicit French
  `aria-label`. Closes the main a11y TODO from last week.

## TODO — manual attention

### Security

- `npm audit --omit=dev` → **0 high, 0 critical, 7 moderate** (unchanged
  from last week). All 7 are transitive of `next` 15.x or `prisma` 5.x and
  require major bumps to clear:
  - `postcss` <8.5.10 (XSS via unescaped `</style>` in stringify output) —
    build-time only, fixed in next major.
  - `next` 15.x (postcss range issue) — wait for 16.x to stabilise.
  - `@clerk/nextjs` / `@vercel/analytics` — both gated behind `next` major.
  - `prisma` 5 → 6 (closes `@hono/node-server` + `hono` middleware bypass).
    Per CLAUDE.md the prisma schema is "historical" / not used at runtime —
    safest action is to either bump to 6 or remove the dep entirely.
- No hardcoded secrets found (`sk_live`, `pk_live`, `AKIA`, BEGIN PRIVATE,
  long `api_key=...`).
- `.env*` confirmed in `.gitignore`.
- No `.env*` file present in working tree.

### SEO

- **Canonical is now set at the layout level** (this run), so every page
  inherits a canonical and OG/Twitter URLs auto-resolve via `metadataBase`.
  This is the global default — for **page-specific canonical** (which
  matters when query params or pagination create variants), still
  recommend setting `alternates.canonical: "<route>"` on:
  - `app/villes/[slug]/page.tsx` and the 4 sub-pages
    (`/climat`, `/quartiers`, `/ecoles`, `/transports`)
  - `app/classements/[slug]/page.tsx`
  - `app/regions/[region]/page.tsx`, `app/departements/[dept]/page.tsx`
  - `app/comparer/[pair]/page.tsx`, `app/guides/[slug]/page.tsx`
- `BASE_URL` in `app/sitemap.ts`, `app/robots.ts`, and now `app/layout.tsx`
  still defaults to `https://mavilleideale.fr` while the live deployment
  is `https://meilleurville.vercel.app/`. If `mavilleideale.fr` is the
  intended prod domain, **set `NEXT_PUBLIC_BASE_URL` on Vercel** to either
  the production domain (best) or to the vercel.app URL. Otherwise
  share-link previews use the wrong host on the live preview deployment.
- `metadata.twitter.site: "@meilleurville"` — still unverified. Confirm
  the handle exists or remove it.
- OG image per sub-route — sub-pages still inherit parent OG. Acceptable;
  per-route OG remains a "nice-to-have".

### Content quality

- Recently shipped pages (since last week's audit on 2026-05-10): essentially
  none new — `git log --since='1 day ago'` shows only this run's commits and
  yesterday's audit. The Cantal/Aubrac guide from f6161ff is from one day
  prior and was already in last week's audit scope. No new thin / stale /
  duplicate content to flag this week.
- Many guide slugs still embed `-2025` (e.g.
  `top-10-villes-teletravail-france-2025`). As noted last week, rotating
  these would break URLs — leave as-is and lean on `updatedAt`. 2026
  refreshes already exist alongside.
- `app/cgu/page.tsx` and `app/confidentialite/page.tsx` still say
  "Dernière mise à jour : 1er avril 2025" — legal text; bump only after
  actual review.
- Internal link audit: all 24 distinct `href="/route"` patterns resolve
  to real routes (static or dynamic `[slug]`).
- No `<img>` tags or `next/image` usage in the codebase; alt-text audit
  remains N/A.

### Bugs

- `npx tsc --noEmit` → **clean**.
- `npm run build` → **2975 pages generated, no errors**, `.next/static`
  total 2.5 MB.
- `npm run lint` → **38 errors, 18 warnings**, unchanged. All errors split
  between two rule families:
  - `react/no-unescaped-entities` (~25): cosmetic, source-only.
  - `react-hooks/set-state-in-effect` (13): real React-19 perf advisory.
    Hot spots remain `components/effects/FavoriteButton.tsx:42,96`,
    `components/CommentSection.tsx:72,78,86`, `components/HeroSection.tsx:65`,
    `app/favoris/FavoritesGrid.tsx:33`, plus a handful of pages reading
    `localStorage` / event-bus state. Fix pattern (per last week):
    `useSyncExternalStore` or a `useState` lazy initialiser. Non-trivial
    behaviour change → still not auto-fixed.
- No test suite present (`npm test` undefined).

### Performance

- `.next/static` = **2.5 MB** total. Within budget.
- `public/` = 5 small SVGs only (≤1.4 KB each). `find public -size +500k`
  returned 0 hits — same as last week.
- No blocking `<script>` in `<head>`. Only font preconnects.
- Map bundle (`maplibre-gl`) scope still worth a manual confirm against
  the home shell.

### Accessibility

- 4 unlabeled search/email inputs **fixed this run** (auto-applied).
- Remaining inputs flagged last week (CostCalculator, CommentSection,
  QuizFlow inputs, CompareTool) all need contextual `aria-label`s. Each
  benefits from copy that matches its surrounding section — not safe to
  auto-apply uniformly. Owner copy decisions needed.
- Contact form (`app/contact/ContactForm.tsx`) actually has proper
  `<label htmlFor>` pairs — last week's flag on it was a false positive.
  Removing from the open list.
- `<html lang="fr">` set in root layout. ✅
- Color contrast — not auditable here; needs Lighthouse / axe in browser.

## Scores (1–10, 10 = perfect)

- Security: 7        (no change — same 7 moderate transitive, all behind major bumps)
- SEO: 8             (+1 — canonical + metadataBase now wired at layout level)
- Content: 8         (no change — corpus unchanged since 2026-05-10)
- Stability: 8       (no change — TS + build clean; same 38 lint errors)
- Performance: 8     (no change — 2.5 MB static, no oversized assets)
- Accessibility: 8   (+1 — 4 critical input labels fixed; remaining are scoped to one-section forms)
- **Overall: 7.8**   (+0.3 vs last week)

## Trend vs last week

`AUDIT-2026-05-10.md` overall: **7.5**. This run: **7.8**.

Progress: closed two of the four "must-fix" TODOs from yesterday (canonical
+ search/email a11y labels). The remaining big items — page-level
canonicals, `NEXT_PUBLIC_BASE_URL` setting on Vercel, prisma major bump,
and the 13 `react-hooks/set-state-in-effect` rewrites — all need either
human judgement or non-trivial refactors and stayed on the manual list.
No new regressions were introduced in the last 24 h (no new commits
between the two audits aside from this run's two fix commits).
