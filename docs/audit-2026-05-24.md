# Weekly audit — 2026-05-24

Scheduled Sunday sweep. Auto-fixes applied where safe; remaining items
listed for follow-up. Distinct from the earlier `audits/ultra-audit-2026-05-24.md`
that ran with a different scope today.

## Summary

- **TypeScript:** clean (`npx tsc --noEmit` → exit 0).
- **Lint:** 41 errors / **0 warnings** (all errors are cosmetic
  `react/no-unescaped-entities`; the 10 unused-variable warnings caught
  this week were fixed in this run).
- **Security:** no hardcoded secrets in the repo (grep of `api_key`,
  `BREVO_API_KEY`, `xkeysib-` patterns).
- **SEO basics:** `generateMetadata` present on all major dynamic route
  families; root layout sets `metadataBase` from env; canonicals and
  hreflang FR/EN/x-default emitted globally.
- **Sitemap:** 16 FR chunks, 8 EN chunks. All recent feature routes
  (`city-match`, `future-you`, `people-like-you`,
  `climat-2040-timelapse`, per-city `empreinte`) are wired in.
- **Newsletter cron:** Vercel Cron set to Sunday 07:00 UTC, protected by
  `CRON_SECRET`. Brevo + SMTP envs documented in `.env.example`.
- **Robots:** `/dashboard` and `/favoris` disallowed; sitemap index +
  per-chunk URLs advertised, indexes derived from `SITEMAP_CHUNK_COUNT`.
- **Mobile nav:** `overflow-x: clip` on `html` and `body` (keeps sticky
  working). Navbar is `sticky top-0 z-50`.
- **Vercel Analytics / Speed Insights:** both mounted in `app/layout.tsx`.

## Auto-fixes shipped in this audit

Cleared every non-cosmetic lint warning — 10 dead imports / unused
destructures with zero behavioural impact:

| File | Removed |
|---|---|
| `app/[locale]/compare/[pair]/page.tsx` | unused `pair` prop on `TripletPage`, unused `i` in `.map` |
| `app/[locale]/expat-return/[slug]/page.tsx` | dead `EXPAT_COUNTRIES` import |
| `app/[locale]/red-flags/themes/[slug]/page.tsx` | dead `RedFlagTheme` type import |
| `app/[locale]/tools/page.tsx` | dead `GLOSSARY_TERMS_COUNT` import |
| `app/[locale]/vacations/[city]/page.tsx` | dead `MONTHS_SHORT`, `BUDGET_TIER_DESC` imports |
| `app/[locale]/vacations/month/[month]/page.tsx` | dead `MONTHS` import, `EN_TO_FR_MONTH_SLUG` map, `INDEX_TO_EN_SLUG` map |

Post-fix: `npx tsc --noEmit` still exit 0; `npm run lint` now reports
**0 warnings**.

## Step-by-step results

### 1. Sync ✅
`git checkout main && git pull origin main` — fast-forwarded from
`e1438eb` to `7bd4e6f`. Working tree clean.

### 2. Build check ✅
`npx tsc --noEmit` → exit 0 after `npm install`.

### 3. SEO & metadata ✅
- `generateMetadata` confirmed on `app/villes/[slug]`,
  `app/classements/[slug]`, `app/comparer/[pair]`,
  `app/red-flags/[slug]`, `app/expat-retour/[pays]`,
  `app/gentrification/[slug]`.
- `app/layout.tsx` resolves `SITE_URL` via `NEXT_PUBLIC_BASE_URL`,
  defaulting to `https://www.mavilleideale.fr` (FR) or
  `https://bestcitiesinfrance.com` (EN). Canonical strategy correct per
  CLAUDE.md (the audit brief mentions `mavilleideal.com` — that is a
  typo, project domain is `mavilleideale.fr`).
- Sitemap covers cities, sub-pages (climat / quartiers / transports /
  ecoles / fiscalite / empreinte), classements, comparer pairs and
  triplets, comparer-regions, regions, departements, guides, red-flags,
  expat-retour, vacances (5 axes), city-match, future-you,
  people-like-you, climat-2040-timelapse. Both locales chunked.
- `app/robots.ts` disallows `/api/`, `/admin/`, `/favoris`,
  `/dashboard`; advertises sitemap index + per-chunk URLs derived from
  `SITEMAP_CHUNK_COUNT`.

### 4. EN locale ✅
- 85 `page.tsx` files under `app/[locale]/`. Layout (`app/[locale]/layout.tsx`)
  enforces only `locale === "en"` via `notFound()`.
- Spot-checked EN metadata on `/`, `/about`, `/faq`: titles and
  descriptions are English-native (no leftover French strings), each
  emits a `canonical` pointing at `bestcitiesinfrance.com`.

### 5. Data integrity ✅
- `data/cities-seed.ts` exports `CITIES_SEED` (length **540**, matches
  CLAUDE.md current count).
- Spot-checked `lyon` (pop 522 250, score 7.10), `marseille` (870 731,
  3.80 — reflects the safety penalty correctly), `annecy` (131 282,
  7.30). Slugs render, scores in band, departments populated.
- `@vercel/analytics/next` and `@vercel/speed-insights/next` both
  imported and mounted in `app/layout.tsx`.

### 6. Newsletter cron ✅
- `vercel.json` cron `0 7 * * 0` → Sunday 07:00 UTC, path
  `/api/cron/newsletter`. Protected by `CRON_SECRET` (cf.
  `app/api/cron/newsletter/route.ts` line 47).
- `.env.example` documents `BREVO_API_KEY`, `BREVO_LIST_ID_FR=4`,
  `BREVO_LIST_ID_EN=5`, `NEWSLETTER_FROM_EMAIL_FR/EN`, `CRON_SECRET`,
  and the contact-form pair.

### 7. Navigation & mobile ✅
- `app/globals.css` lines 40-53: `overflow-x: clip` on `html` and
  `body` (intentional — `hidden` would break sticky). Comment in file
  explains the choice.
- `components/Navbar.tsx` line 201: `sticky top-0 z-50` with smooth
  transitions on `background-color,box-shadow,height`.

### 8. Security ✅
`git grep -E -n "(api[_-]?key\s*=\s*['\"][^'\"]{20,}|BREVO_API_KEY\s*=\s*['\"]|xkeysib-[A-Za-z0-9]{20,})"` over the whole tree (excluding lockfiles and
`.env*`) → no matches. The string `xkeysib-...` appears only in
`.env.example` as a placeholder, which is correct.

`npm audit` reports 11 vulnerabilities (9 moderate, 2 high) — not
inspected in this sweep; dependency upgrades belong to a focused PR with
a build-and-test pass behind it.

## Remaining items (not auto-fixed — surface area too wide for one PR)

- **41 ESLint errors**: all `react/no-unescaped-entities` (apostrophes
  and quotes in JSX). Cosmetic, not a blocker. Worth one dedicated sweep
  later (auto-fixable with `&apos;`/`&quot;`/`&#39;` replacements).
- **`npm audit`**: 11 vulnerabilities (9 moderate, 2 high) — review and
  upgrade transitive deps in a separate PR so the diff is reviewable.
- **`NEXT_PUBLIC_BASE_URL`** still listed in CLAUDE.md "Next priorities"
  as human-blocked on Vercel — out of scope for the agent.
- **Legal page dates** (`app/cgu`, `app/confidentialite`) listed as
  human-blocked in CLAUDE.md (pending legal review).

## Files touched

```
app/[locale]/compare/[pair]/page.tsx
app/[locale]/expat-return/[slug]/page.tsx
app/[locale]/red-flags/themes/[slug]/page.tsx
app/[locale]/tools/page.tsx
app/[locale]/vacations/[city]/page.tsx
app/[locale]/vacations/month/[month]/page.tsx
docs/audit-2026-05-24.md   (this file)
```
