# Journal du maillage interne

Une ligne par run de l'agent de maillage. Format :
`- YYYY-MM-DD app/chemin/de/la/page-cible.tsx (N liens ajoutés)`

- 2026-07-05 app/red-flags/villes-embouteillages-quotidiens/page.tsx (5 liens ajoutés)
- 2026-07-07 app/comparer-departements/page.tsx (5 liens ajoutés)
- 2026-07-09 app/red-flags/villes-erosion-cotiere/page.tsx (5 liens ajoutés)
- 2026-07-12 app/red-flags/villes-manque-de-verdure/page.tsx (5 liens ajoutés)
- 2026-07-14 app/red-flags/villes-logement-introuvable/page.tsx (5 liens ajoutés)
- 2026-07-16 app/red-flags/villes-parking-cauchemar/page.tsx (5 liens ajoutés)
- 2026-07-19 app/pour-qui/sensibles-au-bruit (5 liens ajoutés)
- 2026-07-21 app/red-flags/villes-chauffage-hivernal-couteux/page.tsx (5 liens ajoutés)
- 2026-07-23 app/red-flags/villes-belles-invivables-ete/page.tsx (5 liens ajoutés)
- 2026-07-26 app/red-flags/villes-manque-de-creches/page.tsx (5 liens ajoutés)
- 2026-07-28 app/parent-solo/page.tsx (5 liens ajoutés)
- 2026-07-30 app/villes-qui-grandissent/page.tsx (5 liens ajoutés)
- 2026-08-02 app/red-flags/villes-pauvrete-elevee/page.tsx (5 liens ajoutés)
- 2026-08-04 app/avis/page.tsx (5 liens ajoutés)
- 2026-08-06 app/guides/categorie/[categorie]/page.tsx (6 liens ajoutés — un par thématique)
- 2026-08-09 app/[locale]/reviews/page.tsx (5 liens ajoutés — jumelle EN de /avis, footer seul en entrant)
- 2026-08-11 app/expat-retour/page.tsx (5 liens ajoutés — hub 19 pays, footer seul en entrant contextuel)
- 2026-08-13 app/parcs/page.tsx (5 liens ajoutés — hub F59, Navbar seule en entrant depuis le 27/07)
- 2026-08-16 app/vivre-avec/page.tsx (6 liens ajoutés — hub + 6 tranches de salaire dans le sitemap mais zéro entrant sur tout le site FR : ni Navbar, ni Footer, ni /sommaire, ni /outils)
- 2026-08-18 app/red-flags/villes-qui-se-vident/page.tsx (5 liens ajoutés — thème du 12/08, aucun entrant contextuel : seule la grille générée de /red-flags y menait, quand son pendant /villes-qui-grandissent en compte 6)
- 2026-08-23 app/classements/sans-voiture/page.tsx (5 liens ajoutés — les 10 classements `lib/owner-rankings.ts` forment une clique fermée : ils ne se lient qu'entre eux, le hub `/classements` ne liste que `RANKING_META` et `/sommaire` non plus ; 5 d'entre eux avaient zéro entrant hors clique. Sources : `/velo`, `/villes/[slug]/velo`, `/villes/[slug]/transports`, `/pour-qui/[profil]` (sans-voiture, cyclistes-urbains, etudiants, jeunes-diplomes) et `/classements/[slug]` (mobilite, cyclistes). Au passage : le bouton de `/villes/[slug]/transports` s'intitulait « Classement sans voiture » mais pointait vers `/classements/mobilite` — libellé remis en face de sa destination, le lien vers le vrai classement est ajouté à côté. Restent orphelins hors clique : `securite-femme-seule`, `lien-social`, `jeune-actif`, `famille-proprietaire`.)
- 2026-08-25 app/classements/securite-femme-seule/page.tsx (5 liens ajoutés — deuxième orpheline de la clique `lib/owner-rankings.ts` traitée après `sans-voiture` le 23/08 : zéro entrant hors clique, mesuré `git grep -c "classements/securite-femme-seule"` = 0 fichier. Sources, toutes déjà publiées et sur le même socle SSMSI : `/securite` (carte ajoutée à la section « Voir aussi » existante, à côté de celle vers `securite-nocturne`), `/securite/[macroregion]` ×6, `/villes/[slug]/securite` ×540, `/pour-qui/[profil]` pour `solo-femme` — dont `securiteFemmeSeule` est le poids cardinal à 3,0, donc le classement *est* son critère lu seul — et `celibataires`, qui nomme le profil « femme seule » dans sa propre intro pour dire où il s'arrête, et `/classements/[slug]` pour `securite`. Aucune pondération chiffrée recopiée dans les ancres : le 70/30 reste sur la page cible. Restent orphelins hors clique : `lien-social`, `jeune-actif`, `famille-proprietaire`.)
- 2026-08-27 app/classements/lien-social/page.tsx (5 liens ajoutés — troisième orpheline de la clique `lib/owner-rankings.ts`, après `sans-voiture` le 23/08 et `securite-femme-seule` le 25/08 : zéro entrant, mesuré `git grep -l "lien-social" -- app components lib` = sa propre page + le fichier de définition, rien d'autre. Sources, toutes déjà publiées : `/demographie` (carte ajoutée au « Voir aussi » existant), `/villes/[slug]/demographie` ×540 (troisième ligne de la liste de renvois, à côté de `/villes-qui-grandissent` et `villes-qui-se-vident` — même fichier Insee, autre lecture), `/villes/[slug]/mentalite-locale` ×540 (paragraphe en flux : les six dimensions décrivent l'ambiance, pas avec qui on la partage), `/classements/retraite` (bloc « Classement voisin », pattern déjà en place pour `mobilite`/`cyclistes` et `securite`) et `/pour-qui/[profil]` pour `celibataires` — dont l'intro dérive déjà sa « densité de célibataires » de ce score —, `retraites`, `futurs-retraites`, `neo-ruraux` et `expat-retour`, les profils qui arrivent quelque part avec un cercle social à reconstruire. Aucune valeur du barème recopiée dans les ancres ; chaque ancre dit en revanche que la table ne couvre pas tous les départements et que les villes sans valeur propre sont écartées plutôt que triées à égalité — c'est la limite que la page cible affiche elle-même. Reste orpheline hors clique : `famille-proprietaire`.)
- 2026-08-30 app/classements/famille-proprietaire/page.tsx (5 liens ajoutés — quatrième et dernière orpheline de la clique `lib/owner-rankings.ts`, après `sans-voiture` le 23/08, `securite-femme-seule` le 25/08 et `lien-social` le 27/08 : zéro entrant, mesuré `git grep -l "classements/famille-proprietaire" -- app components lib` = sa propre page seule. Sources, toutes déjà publiées : `/classements/famille` (bloc « Classement voisin », pattern déjà en place pour `mobilite`/`cyclistes`, `securite` et `retraite`), `/pour-qui/[profil]` pour `familles-avec-enfants`, `jeunes-parents`, `familles-nombreuses` et `familles-monoparentales` — les quatre profils dont la pondération repose sur écoles + sécurité + verdure —, `/villes/[slug]/ecoles` ×540 (troisième bouton du pied de page, à côté du classement famille officiel et du classement étudiant), `/villes/[slug]/cout-de-la-vie` ×540 (lien en flux dans la note d'hypothèses qui signalait déjà que « retraités et familles ont une structure de coûts différente » sans mener nulle part) et `/parent-solo` (carte dans la grille « Aller plus loin » existante). L'angle des cinq ancres est le même parce que c'est ce qui distingue réellement la page cible de `/classements/famille` : le composite ne retient qu'écoles, sécurité et nature puis retranche une pénalité aux villes les plus chères, quand le classement officiel pondère aussi coût et transport et remonte donc les métropoles bien desservies. Aucun chiffre ni pondération recopiés dans les ancres ; deux d'entre elles disent en revanche que les ex æquo sont publiés par paliers et non en rangs fabriqués, ce que la page cible affiche elle-même. ⚠️ « propriétaire » désigne ici un score maison, pas l'accession à la propriété : aucune ancre ne parle d'acheter. La clique `lib/owner-rankings.ts` est désormais entièrement traitée, à une exception près restée orpheline hors clique : `jeune-actif` — la note du 27/08 l'avait retirée de sa liste de restants par erreur, elle n'a jamais reçu d'entrant, mesuré `git grep -l "classements/jeune-actif"` = 0 fichier.)
- 2026-09-01 app/classements/jeune-actif/page.tsx (5 liens ajoutés — la dernière orpheline de la clique `lib/owner-rankings.ts`, que la note du 30/08 avait signalée après l'avoir retirée par erreur de sa liste de restants le 27/08 : zéro entrant, mesuré `git grep -l "classements/jeune-actif" -- app components lib` = 0 fichier avant ce run (le slug n'apparaissait que dans sa propre page et dans `lib/owner-rankings.ts` / `lib/profile-pages.ts`, deux définitions, pas des liens). Sources, toutes déjà publiées : `/classements/jeunes-actifs` (bloc « Classement voisin », pattern déjà en place pour `mobilite`/`cyclistes`, `securite`, `retraite` et `famille`), `/pour-qui/[profil]` pour `jeunes-actifs`, `jeunes-diplomes` et `celibataires` — les trois profils dont `jeuneActif` est le poids cardinal (2,5 pour le premier, 2,0 pour les deux autres, celui des célibataires dérivant déjà sa « vitalité du bassin jeune-actif » de ce score) —, `/villes/[slug]/emploi` ×540 (paragraphe en flux sous le renvoi existant vers `/emploi`), `/villes/[slug]/teletravail` ×540 (badge à côté de celui vers `teletravail-proprietaire`, le composite pesant `remoteWork` pour un tiers) et le hub `/emploi` (carte dans la grille « Voir aussi » existante). L'angle des cinq ancres est le même parce que c'est ce qui distingue réellement la cible du classement officiel `/classements/jeunes-actifs` : celui-ci pondère cinq axes dont le transport et la qualité de vie, ce qui suppose une métropole desservie ; le composite propriétaire n'en retient que trois — culture, télétravail, coût — **à poids égal et sans axe transport**, puis ajoute un bonus forfaitaire au-dessus de 100 000 habitants, si bien que des communes petites, bon marché et culturellement actives y côtoient les capitales régionales (le premier palier mêle Strasbourg à Autun et Vaison-la-Romaine). Aucun chiffre du barème recopié dans les ancres hors le seuil de population, qui est la règle du bonus et non un résultat ; deux ancres disent en revanche que les ex æquo sont publiés par paliers et non en rangs fabriqués, et une troisième que la page s'annonce elle-même comme un indicateur de substitution en attente d'une mesure directe de la tranche 25-35 ans — c'est ce que sa propre méthodologie affiche. La clique `lib/owner-rankings.ts` est désormais entièrement traitée : `sans-voiture` le 23/08, `securite-femme-seule` le 25/08, `lien-social` le 27/08, `famille-proprietaire` le 30/08 et `jeune-actif` ce run. Prochain gisement à ouvrir ailleurs — la clique n'a plus d'orpheline.)
- 2026-09-03 app/pour-qui/travailleurs-frontaliers (5 liens ajoutés — premier run hors de la clique `lib/owner-rankings.ts`, refermée le 01/09. Nouveau gisement ouvert : les 35 profils de `lib/profile-pages.ts` ont exactement le même défaut que les classements propriétaires — la grille « autres profils » du bas de `/pour-qui/[profil]` les relie tous entre eux, si bien qu'un `grep` les montre liés alors que **32 sur 35 n'ont aucun entrant contextuel** ; les trois exceptions sont `sensibles-au-bruit` (4 entrants), `asthmatiques-allergiques` (2) et `familles-monoparentales` (2, depuis `/parent-solo`). Cible retenue : `travailleurs-frontaliers`, livré le 31/08, mesuré `git grep -l "travailleurs-frontaliers" -- app components lib` = **0 fichier** avant ce run (le slug n'apparaissait que dans `lib/profile-pages.ts`, sa définition, pas un lien) ; c'est aussi le seul profil du site dont le critère cardinal pointe hors de France, donc celui dont l'absence de maillage se voit le moins par ressemblance de sujet avec le reste du corpus. Sources, toutes déjà publiées : `/emploi` (carte dans la grille « Voir aussi » existante), `/salaire-equivalent` (phrase en flux, à la suite du renvoi existant vers `/expat-retour` : la page traitait déjà le cas d'un salaire libellé en devise, mais seulement pour un retour définitif — être payé en francs suisses **sans** avoir quitté la France est un troisième cas, qu'aucune des deux pages ne couvrait), `/villes/[slug]/emploi`, `/villes/[slug]/transports` et `/villes/[slug]/teletravail`.
  ⚠️ **Les trois sous-pages ville rendent leur lien sous condition, et c'est le point de méthode du run** : `borderCommute()` (`lib/profile-pages.ts`) rend le plus proche des quatorze pôles étrangers retenus, ou `null` au-delà de 110 km — le renvoi n'apparaît donc que sur les **89 villes sur 540** qui sont dans le champ du barème, et l'ancre nomme le pôle réel (Bâle, Mouscron, Genève, Sarrebruck, Monaco…). Les runs précédents posaient des liens inconditionnels sur les 540 ; ici un renvoi frontalier sur la page emploi de Brest ou de Cayenne aurait été du bruit, et le nommer « le plus proche des pôles suivis par le site » aurait été faux. `borderCommute` est importée dans trois **composants serveur** : aucune de ces pages n'est `"use client"`, donc rien de `lib/profile-pages.ts` (≈ 200 Ko de prose) ne part dans le bundle — la règle inverse vaut toujours pour `PeopleLikeYouClient`.
  Les trois angles sont distincts parce que les trois pages le sont : sur `emploi`, le bassin décrit s'arrête à la frontière et ni le salaire ni la caisse maladie n'en dépendent plus ; sur `transports`, passer la frontière en tram ou en TER plutôt qu'en seconde voiture ; sur `teletravail`, le fait que les jours passés à la maison ne sont plus un confort mais un paramètre fiscal. **Aucun des trois plafonds (40 % franco-suisse, 34 jours luxembourgeois, 49,9 % social européen) n'est recopié dans une ancre** — les confondre est l'erreur la plus coûteuse du dossier et elle se traite sur la page cible, pas dans un lien. Aucun kilométrage, aucun loyer, aucune note recopiés non plus. Restent sans entrant contextuel, pour les prochains runs de ce gisement : `suivi-medical-regulier` et `navetteurs-hybrides` (les deux autres profils récents, 0 entrant chacun), puis les 27 profils historiques.
- 2026-09-06 app/pour-qui/suivi-medical-regulier (5 liens ajoutés — deuxième run du gisement « profils » ouvert le 03/09, après `travailleurs-frontaliers` : sur les 35 profils de `lib/profile-pages.ts`, la grille « autres profils » du bas de `/pour-qui/[profil]` les relie tous entre eux, si bien qu'un `grep` les montre liés alors que la plupart n'ont aucun entrant contextuel. Cible retenue : `suivi-medical-regulier`, mesuré `git grep -l "suivi-medical-regulier" -- app components lib` = **1 fichier**, `lib/profile-pages.ts`, sa définition — donc **zéro entrant** avant ce run. C'est aussi le seul profil du site dont le critère cardinal est l'accès aux soins : `lib/healthcare-access.ts` (F47) alimentait `/sante`, `/sante/[macroregion]`, `/villes/[slug]/sante` et le red flag `villes-desert-medical` sans qu'aucune de ces quatre surfaces ne mène au profil qui repondère la mesure. Sources, toutes déjà publiées et sur ce même moteur : `/sante` (carte ajoutée à la grille « Voir aussi » existante, à côté de celles vers `/environnement` et le red flag), `/sante/[macroregion]` ×6 et `/villes/[slug]/sante` ×540 (seconde ligne de renvoi sous le lien existant vers le classement national), `/red-flags` (paragraphe en flux à la suite de ceux qui appairent déjà un red flag à sa lecture par profil : le red flag dit où l'accès s'effondre, il ne dit pas où aller quand on ne peut pas s'en passer) et `/pour-qui/[profil]` pour `mobilite-reduite`, `proches-aidants` et `asthmatiques-allergiques` — les trois profils que l'intro de la cible nomme elle-même pour dire où elle s'arrête, et dont **aucun ne pondère `healthcareAccess`** (vérifié sur `PROFILE_PAGES` : transport 2,5 / safety 2,0 / qualiteAir 3,0 en tête respectivement), ce qui est exactement ce qui justifie le renvoi.
  L'angle des cinq ancres est le même parce que c'est ce qui distingue réellement la cible des quatre surfaces santé : celles-ci publient **l'accès lu seul** (généralistes 35 %, spécialistes 25 %, urgences 25 %, pharmacies 15 %), la cible le repondère avec ce qu'un rendez-vous mensuel ou hebdomadaire coûte vraiment — le trajet répété, le reste à charge hors ALD, la résistance à la canicule. **Aucune pondération, aucune note, aucun loyer, aucun rang recopiés dans les ancres** ; trois d'entre elles disent en revanche que l'indicateur est une **estimation** construite depuis le département, la taille de la commune et son statut hospitalier, et non un relevé de cabinets — c'est l'avertissement que la page cible affiche elle-même avant son premier rang, et le taire dans un lien qui promet des villes où se soigner aurait été le pire endroit où l'omettre. ⚠️ Le renvoi des trois sous-pages/hubs santé est **inconditionnel**, contrairement à celui du 03/09 : `computeHealthcareAccess()` rend une valeur sur les 540 villes (c'est une estimation, pas une donnée manquante), donc il n'y a pas de ville hors champ à exclure — la retenue du run précédent tenait au `null` au-delà de 110 km de `borderCommute()`, pas à une règle générale. Les trois pages sources sont des composants serveur : rien de `lib/profile-pages.ts` (≈ 200 Ko de prose) ne part dans le bundle. Restent sans entrant contextuel, pour les prochains runs de ce gisement : `navetteurs-hybrides` (1 occurrence, dans `app/[locale]/cities/[slug]/honest-review/page.tsx`, à vérifier — c'est peut-être un libellé et non un lien), puis les 27 profils historiques.)
- 2026-09-08 app/pour-qui/navetteurs-hybrides (5 liens ajoutés — troisième run du gisement « profils » ouvert le 03/09, après `travailleurs-frontaliers` le 03/09 et `suivi-medical-regulier` le 06/09 : sur les 35 profils de `lib/profile-pages.ts`, la grille « autres profils » du bas de `/pour-qui/[profil]` les relie tous entre eux, si bien qu'un `grep` les montre liés alors que la plupart n'ont aucun entrant contextuel. Cible retenue : `navetteurs-hybrides`, la dernière des trois profils récents, mesuré `git grep -l "navetteurs-hybrides" -- app components lib` = **2 fichiers** avant ce run, dont aucun n'est un lien : `lib/profile-pages.ts` (sa définition) et `app/[locale]/cities/[slug]/honest-review/page.tsx`, où le slug est une **clé d'une table de libellés EN** (`"Hybrid commuters (2-3 days on site)"`) — c'est le doute que la note du 06/09 demandait de lever, il est levé, l'entrée n'a jamais été un lien. Donc **zéro entrant** avant ce run.
  C'est aussi le seul profil du site dont le critère cardinal est la **distance à un bassin d'emploi** : `metroAccess` / `metroAccessCommute()` n'alimente aucune autre surface, si bien que ni `/classements/teletravail`, ni `/emploi`, ni les deux sous-pages ville qui parlent du même sujet ne menaient au profil qui mesure ce que les jours au bureau coûtent en trajet.
  Sources, toutes déjà publiées : `/villes/[slug]/teletravail` (badge dans la rangée de renvois existante, à côté de `teletravail-proprietaire`, `jeune-actif` et du renvoi frontalier du 03/09), `/villes/[slug]/transports` (phrase en flux à la suite de la note d'estimation de la section « Trajet domicile-travail estimé », qui décrit le trajet **quotidien et intra-communal** — l'hybride est l'autre trajet, celui du mardi et du jeudi, et les deux ne se déduisent pas l'un de l'autre), `/emploi` (carte dans la grille « Voir aussi » existante), `/classements/teletravail` (bloc « Profil voisin », variante du pattern « Classement voisin » déjà en place pour `mobilite`/`cyclistes`, `securite`, `retraite`, `famille` et `jeunes-actifs` — c'est un profil et non un classement, l'intitulé le dit) et `/pour-qui/[profil]` pour `teletravailleurs`, `freelances`, `sans-voiture` et `cyclistes-urbains`, les quatre profils que l'intro de la cible nomme elle-même pour dire où elle s'arrête, et dont **aucun ne pondère `metroAccess`** (vérifié par exécution sur `PROFILE_PAGES` : remoteWork 2,5 pour les deux premiers, sansVoiture 3,0 et cyclingMobility 3,0 pour les deux autres).
  L'angle des cinq ancres est le même parce que c'est ce qui distingue réellement la cible de tout ce qui l'entoure : les surfaces télétravail notent la ville pour les jours passés **à la maison** — fibre, coworking, coût, qualité de vie — et `/classements/teletravail` ne mesure aucune distance, ce qui est le bon barème quand on ne revient jamais ; les surfaces mobilité mesurent le déplacement **à l'intérieur** d'une ville. La cible part du trajet vers le plus proche des douze grands bassins et le confronte au loyer, parce que l'hybride revient à acheter du mètre carré avec du temps de transport. **Aucun loyer, aucune note, aucun rang, aucune pondération et aucune minute ne sont recopiés dans les ancres** — même retenue qu'au 03/09, où le pôle était nommé mais pas le kilométrage ; trois ancres disent en revanche que le trajet est une **estimation**, qu'elle **sous-évalue le relief** et qu'elle vaut **zéro en Corse et dans les DROM faute de navette hebdomadaire**, ce que la page cible affiche elle-même.
  ⚠️ **Les deux sous-pages ville rendent leur lien sous condition, et la condition est le barème de la cible lui-même, pas un seuil inventé** : `metroAccess(city) > 0`, c'est-à-dire `metroAccessCommute()` non `null` (24 villes des DROM et de Corse, où la navette n'existe pas — une mesure, pas une donnée manquante) **et** un trajet sous les 2 h 30 (21 villes de plus, de Cherbourg à Guéret, que le barème met déjà à 0). Mesuré par exécution : **495 villes sur 540** portent le renvoi, 45 ne le portent pas. Sur `/transports` la condition est resserrée à `hub !== city.slug` (483 villes) : la phrase suppose un bureau ailleurs, elle n'a pas de sens sur la page des **12 villes-pôles** elles-mêmes ; sur `/teletravail` ces 12 gardent le badge avec une formulation propre, puisqu'y habiter est le cas le plus favorable du classement. Les deux pages sont des **composants serveur** — rien de `lib/profile-pages.ts` (≈ 200 Ko de prose) ne part dans le bundle, la règle inverse valant toujours pour `PeopleLikeYouClient`.
  Un seul export ajouté à la lib, `HUB_LABEL` (déjà déclaré, seulement rendu public) : le `hub` rendu par `metroAccessCommute()` est un **slug**, pas un libellé, et le rendre lisible en refaisant un `CITIES_SEED.find` sur chacune des deux pages aurait dupliqué une table qui existe. Aucun poids, aucun barème, aucune surface n'ont bougé. Restent sans entrant contextuel, pour les prochains runs de ce gisement : les 27 profils historiques — les trois profils récents (31/08, 17/08 et celui-ci) sont désormais tous maillés.
- 2026-09-13 app/pour-qui/famille-a-l-etranger (5 liens ajoutés — quatrième run du gisement « profils » ouvert le 03/09, après `travailleurs-frontaliers` le 03/09, `suivi-medical-regulier` le 06/09 et `navetteurs-hybrides` le 08/09 : sur les 36 profils de `lib/profile-pages.ts`, la grille « autres profils » du bas de `/pour-qui/[profil]` les relie tous entre eux, si bien qu'un `grep` les montre liés alors que la plupart n'ont aucun entrant contextuel. Comptage fait ce run sur les 36 slugs, `git grep -l "<slug>" -- app components lib` moins sa propre définition : **quatre profils sont à zéro entrant** — `amateurs-de-culture`, `amateurs-de-montagne`, `amateurs-de-plein-air` et `famille-a-l-etranger`. Cible retenue : `famille-a-l-etranger`, livré le 11/09, donc le hub le plus récent du site et celui que les trois autres candidats, tous historiques, ne battaient sur aucun critère.
  C'est aussi le seul profil du site dont le critère cardinal est **l'accès aérien** : `airportAccess` / `airportAccessHub()` (51 plateformes en dur, `lib/profile-pages.ts`) n'alimente aucune autre surface, et **aucun des 35 autres profils ne le pondère** (vérifié par exécution sur `PROFILE_PAGES`). Il n'existait donc, avant ce run, aucun chemin depuis une page parlant d'avion, de frontière ou de retour d'expatriation vers la page qui mesure cet axe.
  Sources, toutes déjà publiées : `/pour-qui/[profil]` pour `expat-retour`, `travailleurs-frontaliers` et `navetteurs-hybrides` — les trois profils que l'intro de la cible nomme elle-même pour dire où elle s'arrête, et dont aucun ne pondère `airportAccess` (life 2,5 / borderAccess 3,0 / metroAccess 3,0 en tête respectivement) —, le hub `/expat-retour` (carte dans la pile existante, entre le CTA quiz et le guide admin), `/expat-retour/depuis-[pays]` ×23 (paragraphe en flux sous la grille « Villes françaises les plus adaptées », dont l'intro annonce déjà « transports vers l'ancien lieu de vie » sans mener nulle part), `/villes/[slug]/transports` ×540 (badge nommant la plateforme réelle, dans la rangée de pied de page à côté du badge frontalier du 03/09) et `/villes/[slug]/bruit` (paragraphe en flux sous les quatre dimensions suivies).
  L'angle des cinq ancres est le même parce que c'est ce qui distingue réellement la cible de tout ce qui l'entoure : les fiches expat traitent un retour qu'on **solde**, le profil frontalier une frontière franchie tous les matins, le profil hybride douze bassins d'emploi français par le rail et la route — trois barèmes qui supposent une vie tenant d'un seul côté. La cible part de l'inverse, une vie restée à cheval sur deux pays, et mesure l'accès aérien **international** : pas l'aéroport le plus proche, mais ce qu'on peut y prendre un mardi de novembre. **Aucun chiffre n'est recopié dans une ancre** — ni note, ni rang, ni pondération, ni trafic passagers, ni kilométrage (même retenue qu'au 03/09, où le pôle frontalier était nommé sans sa distance) ; trois ancres disent en revanche que la distance est **estimée à vol d'oiseau** et **sous-évalue le relief**, ce que la page cible affiche elle-même.
  ⚠️ **Le renvoi de `/villes/[slug]/transports` est inconditionnel, à l'inverse du badge frontalier posé au même endroit le 03/09, et la différence est dans le barème et non dans le goût** : `borderCommute()` rend `null` au-delà de 110 km, donc 89 villes sur 540 seulement ; `airportAccessHub()` rend une plateforme pour **540 villes sur 540** (mesuré par exécution), le 0,0 de Saint-Laurent-du-Maroni — 249 km de Cayenne — étant une **mesure** et non une donnée manquante. 44 plateformes distinctes sont effectivement rattachées, Roissy pour 118 villes, Orly 43, Lyon-Saint-Exupéry 38. Le badge nomme la plateforme telle que le barème la choisit, et « la mieux placée » n'est pas « la plus proche » : le produit plafond × décroissance donne Roissy à Compiègne (62 km) et l'EuroAirport à Mulhouse (27 km) plutôt qu'Entzheim. ⚠️ Ne pas « corriger » ce badge en le branchant sur `computeCityDistances(city).airport` (`lib/distances`) : cette liste-là ne compte que dix plateformes métropolitaines, sans la Corse, sans l'outre-mer et sans Bâle-Mulhouse — le commentaire de `lib/profile-pages.ts` l'explique depuis l'origine.
  ⚠️ **Le renvoi de `/villes/[slug]/bruit` est conditionné par la dimension aérienne de la page elle-même**, `n.aircraft.level !== "faible"`, soit **44 villes sur 540** (mesuré : 29 en « élevé », 15 en « fort ») — un seuil lu dans `lib/noise-exposure.ts`, pas inventé pour l'occasion. C'est le seul des cinq liens dont l'angle n'est pas le voyage mais son revers, et il est écrit dans les deux sens : vivre sous une trajectoire est aussi ce qui rend l'aller-retour possible dans la journée, **et** le barème de la cible ne pondère pas le bruit des avions. Les deux pages renvoient au **plan d'exposition au bruit**, qui est le document opposable — le taire dans un lien qui vante la proximité d'une piste aurait été le pire endroit où l'omettre.
  Les cinq pages sources sont des **composants serveur** : rien de `lib/profile-pages.ts` (≈ 200 Ko de prose) ne part dans le bundle, la règle inverse valant toujours pour `PeopleLikeYouClient`. Aucun export ajouté à la lib — `airportAccessHub()` était déjà public. Aucun poids, aucun barème, aucune surface, aucun `generateMetadata()` n'ont bougé. Contrôles : `npx tsc --noEmit` **propre**, `npm run integrity` (13 gardes vertes, dont `env quartet`, `moteurs` et `classements`), `npx eslint` sur les cinq fichiers **propre**, vérification d'encodage (aucun `m2` / `EUR` / `deg` ascii, aucun mojibake) et contrôle d'ancres imbriquées (aucun `<Link>` ajouté à l'intérieur d'un `<a>` existant). `npm run build` **non lancé, volontairement** (cf. § Commands de CLAUDE.md depuis le batch 27). Restent sans entrant contextuel, pour les prochains runs de ce gisement : **`amateurs-de-culture`, `amateurs-de-montagne` et `amateurs-de-plein-air`** — trois profils historiques à zéro entrant, qui ont l'avantage d'avoir des surfaces sources évidentes (`/villes/[slug]/a-faire`, `/villes/[slug]/sport`, `/classements/culture`, `/classements/nature`) là où les trois profils récents n'en avaient aucune —, puis les 24 profils historiques à un seul entrant.)
- 2026-09-17 app/pour-qui/amateurs-de-montagne (5 liens ajoutés — cinquième run du gisement « profils » ouvert le 03/09, après `travailleurs-frontaliers` le 03/09, `suivi-medical-regulier` le 06/09, `navetteurs-hybrides` le 08/09 et `famille-a-l-etranger` le 13/09.
  ⚠️ **Le recensement des runs précédents mesurait avec un pathspec qui rate les fichiers à la racine de `components/` et de `lib/`, et ce run l'a corrigé.** `git grep -- 'app/**/*.tsx' 'components/**/*.tsx' 'lib/**/*.ts'` (la forme prescrite, reprise de run en run) ne fait correspondre `**/` qu'à un chemin comportant **au moins un dossier intermédiaire** : `components/DistancesCard.tsx` et `lib/people-like-you-en.ts` n'entraient dans aucun comptage. Le pathspec juste est `-- app components lib`, sans motif. Mesuré avec lui sur les 36 slugs de `lib/profile-pages.ts`, à `HEAD` et hors définition : **`amateurs-de-culture` et `amateurs-de-montagne` sont à zéro fichier**, et deux des trois « zéro » annoncés le 13/09 avaient en réalité une occurrence, dans les deux cas **une clé et non un lien** — `amateurs-de-plein-air` dans la table de libellés EN de `lib/people-like-you-en.ts` (`"Outdoor lovers"`), `amateurs-de-littoral` dans son propre `profile.slug === …` de `app/pour-qui/[profil]/page.tsx`. Même faux positif que celui levé le 06/09 pour `navetteurs-hybrides`. Les quatre étaient donc bien sans entrant réel ; c'est le compte, pas la conclusion, qui était faux.
  Cible retenue : `amateurs-de-montagne`, **zéro fichier hors sa définition**, vérifié à `HEAD` avec le pathspec corrigé. C'est aussi le seul profil du site dont le critère cardinal est la proximité du relief : `mountainProximity()` (`lib/profile-pages.ts`) n'est lu par **aucune autre surface du dépôt** (`git grep -l mountainProximity -- app components lib` = ce seul fichier) et **aucun des 35 autres profils ne le pondère**. Il n'existait donc aucun chemin depuis une page parlant de montagne vers la page qui la mesure.
  Sources, toutes déjà publiées : le hub `/sport` (carte dans la grille « Voir aussi » existante), `/villes/[slug]/sport` (paragraphe en flux sous le renvoi existant vers le classement national), `/classements/nature` (bloc « Profil voisin », variante du pattern « Classement voisin » déjà en place pour `mobilite`/`cyclistes`, `securite`, `retraite`, `famille`, `jeunes-actifs` et `teletravail`), `/pour-qui/[profil]` pour `amateurs-de-plein-air`, `amateurs-de-littoral` et `sportifs` — les deux premiers étant nommés par l'intro de la cible elle-même pour dire où elle s'arrête, le troisième mesurant les équipements et les clubs et non le relief, et **aucun des trois ne pondérant `mountainProximity`** (vérifié par exécution sur `PROFILE_PAGES` : nature 3,0 · coastalProximity 3,0 · sportLeisure 3,0 en tête respectivement) — et `components/DistancesCard.tsx`, la carte « Distances clés » du rail de `/villes/[slug]`, qui **affichait la ligne « Montagne — massif — X km » depuis toujours sans mener nulle part**.
  L'angle des cinq ancres est le même parce que c'est ce qui distingue réellement la cible de tout ce qui l'entoure : partout ailleurs le relief est **dilué dans un composite**. Le cadre outdoor du composite sport (30 %) additionne montagne, façade côtière, massif forestier et plan d'eau, et deux atouts quelconques suffisent à faire monter la note ; l'axe `nature` du seed compte forêts, lacs, montagne et mer ensemble. **Mesuré ce run pour vérifier que l'ancre ne surjoue pas : 322 paires (ville à ≤ 10 km de la mer et sans montagne) / (ville à score massif ≥ 8 et à plus de 100 km de la mer) partagent exactement la même note `nature`** — Brest, à 1 km de la mer, sort à 6,4 comme Tarbes, à 15 km des Pyrénées. La cible, elle, repart d'une seule mesure.
  **Aucun chiffre n'est recopié dans une ancre** — ni note, ni rang, ni pondération, ni kilométrage, même retenue qu'au 03/09 et au 13/09 ; quatre ancres disent en revanche que la distance est mesurée **à vol d'oiseau depuis la porte d'entrée basse du massif et non depuis un sommet**, donc qu'elle **sous-évalue le temps de route en relief**, et une que le profil **vaut zéro dans les DROM, dont aucun massif ne figure dans la table** (vérifié par exécution : les 18 villes ultramarines sortent à 0, `MOUNTAINS` de `lib/distances.ts` ne porte que sept portes métropolitaines, Corse comprise, si bien que Saint-Denis de La Réunion est rattachée à Corte, à 8 473 km).
  ⚠️ **Deux des cinq liens sont conditionnels, et la condition est le barème de la cible, pas un seuil inventé** : sur `/villes/[slug]/sport` le renvoi n'apparaît que si `mountainProximity(city) > 0`, c'est-à-dire une porte de massif à moins de 250 km — mesuré par exécution, **279 villes sur 540**, 261 non. Sur les 279, le nom du massif est rendu depuis `computeCityDistances(city).mountain`, avec deux normalisations : le libellé `"Corse — Massif"` est ramené à `"Corse"` (sans quoi son tiret cadratin entrait en collision avec la ponctuation de la phrase), et la mention « entrée par X » est **supprimée quand la porte est la ville elle-même** — sinon Gap, Corte, Albertville, Lourdes, Gérardmer, Lons-le-Saunier et Clermont-Ferrand lisaient « depuis Gap … entrée par Gap ». Les 279 phrases ont été rendues hors build et relues sur échantillon.
  ⚠️ **Le lien de `components/DistancesCard.tsx` est FR seulement, et c'est une contrainte de route, pas de goût** : `app/[locale]/for-who/[slug]` ne publie qu'une **sélection de 13 profils** (vérifié : `EN_PROFILES`, 13 entrées, `amateurs-de-montagne` n'en fait pas partie), donc un lien depuis la carte anglaise tomberait en 404. Le garde est `locale !== "en"`. ⚠️ Et ce composant est monté par `CityProfile`, qui est `"use client"` : le seuil de 250 km y est donc **repris en clair avec le commentaire qui le justifie**, plutôt qu'importé de `lib/profile-pages.ts`, dont les ~200 Ko de prose partiraient sinon dans le bundle des 540 pages ville des deux locales. Les quatre autres pages sources sont des **composants serveur**, elles importent la lib sans coût client.
  Aucun export ajouté à la lib (`mountainProximity` était déjà public), aucun poids, aucun barème, aucune surface, aucun `generateMetadata()` n'ont bougé. Contrôles : `npx tsc --noEmit` **propre**, `npm run integrity` (14 gardes vertes, dont `env quartet`, `moteurs`, `classements` et `expat`), `npx eslint` sur les cinq fichiers **propre**, vérification d'encodage (aucun `m2` / `EUR` / `deg` ascii, aucun mojibake), contrôle d'ancres imbriquées (aucun `<Link>` ajouté à l'intérieur d'un `<a>` existant) et compte d'em-dash dans la copie rendue : **1** (les sept autres du diff sont des commentaires de code ou le motif de `replace`). `npm run build` **non lancé, volontairement** (cf. § Commands de CLAUDE.md depuis le batch 27).
  Restent sans entrant réel, pour les prochains runs de ce gisement : **`amateurs-de-culture`** (zéro fichier, et des surfaces sources évidentes : `/classements/culture`, `/villes/[slug]/a-faire`, les 268 guides `10-choses-a-faire`), **`amateurs-de-plein-air`** et **`amateurs-de-littoral`** (une occurrence chacun, ni l'une ni l'autre n'étant un lien — voir ci-dessus), puis les profils historiques à un seul entrant, **à recompter avec le pathspec corrigé** et non avec celui des runs précédents.)
