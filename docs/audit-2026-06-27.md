# Weekly audit — 2026-06-27

Scheduled audit (run a day early — Sat). Sync → tsc → SEO/metadata → EN locale
→ data → cron → nav → security. Stack is Next.js 16.2.6 + Cloudflare Workers
Static Assets (not the Next 14 / Vercel template the audit spec was written
against — checks adapted to actual deployment, same as the last six weekly
reports).

## TL;DR

- **Tree clean, tsc clean.** `npx tsc --noEmit` exits 0.
- **No bugs found this week.** 9 commits, 16 files, ~1 700 LoC — all
  additive (new internet hub FR + EN, 2 red-flag themes, expat-retour:irlande,
  2 pour-qui profiles). No regressions in canonical, sitemap, or cron wiring.
- **One safe doc fix applied:** CLAUDE.md `npm run lint` comment said
  "25 errors remaining" — stale since at least 2026-06-20 (actual 231).
  Bumped to current numbers + rule shape.
- **No secrets leaked.** API key regex scan over tracked files = 0 hits.
- **231 lint errors / 27 warnings** — same shape as recent weeks
  (`@next/next/no-html-link-for-pages` dominates at 156, harmless under
  `output:"export"`). Listed below; not auto-fixed.

## 1. Sync

`origin/main` was force-pushed since the previous session (50 commits replaced;
prior tip was `4a023ca`, new tip is `8feb5b0`). `git reset --hard origin/main`
clean — detached HEAD had no in-progress work.

9 commits land between last audit (`f1345f3`) and now:

| Commit | Subject |
|---|---|
| `8feb5b0` | EN `/internet-quality` hub + 6 macro-régions |
| `0e0f8a4` | red-flags: `villes-deficit-soleil-hiver` |
| `71cec31` | FR `/internet` national hub + 6 macro-régions |
| `2bd2d0d` | expat-retour: `depuis-irlande` |
| `cc7d25d` | pour-qui: `cyclistes-urbains` (27e profil) |
| `8def696` | pour-qui: `jeunes-diplomes` (26e profil) |
| `ef27b32` | red-flags: `villes-climat-2040-deteriore` |
| `f1345f3` | weekly ultra-audit — clean |
| `52956e9` | audit 2026-06-20 (purge 9 self-redirects) |

## 2. Build check

- `npx tsc --noEmit` → **exit 0**. Strict TS pass clean. (Required
  `npm install` first — node_modules absent in fresh container; not a
  repo issue.)
- `npm run lint` → **231 errors / 27 warnings**, breakdown:

| Rule                                       | Count | Notes |
|--------------------------------------------|-------|-------|
| `@next/next/no-html-link-for-pages`        | 156   | EN locale pages + a few FR forms use `<a href="/…">`. Under `output:"export"` next/link gives no client-side prefetch benefit, so this is cosmetic — same as the last 5 audits. |
| `react/no-unescaped-entities`              | 69    | Pre-existing residue flagged in CLAUDE.md. |
| `@typescript-eslint/no-unused-vars`        | 19    | Mostly scripts + 2 lib files. |
| `react-hooks/set-state-in-effect`          | 6     | `ConnexionForm`, `CityProfile`, navbar, callback handler. Hydration / token-gating patterns; per-callsite review needed. |
| `react-hooks/exhaustive-deps`              | 6     | Same files, same pattern. |
| `import/no-anonymous-default-export`       | 1     | `worker/index.ts` line 727 — Worker module convention, intentional. |

Lint count drifted up by 4 vs the 2026-06-20 audit (227 → 231) — driven by the
new EN `/internet-quality` hub + 6 macro-region pages, which use plain `<a>`
tags for cross-route navigation (same pattern as the existing `/cycling`,
`/sport`, `/employment` EN hubs). Not a regression — consistent with the
established pattern, just inflates the cosmetic count.

None of the lint hits cause runtime bugs or break SSG.

## 3. SEO & metadata

- `app/robots.ts` — noindex strategy unchanged: `/auth` disallowed
  (magic-link callbacks shouldn't be crawled); `/dashboard`, `/favoris`,
  `/mes-villes`, `/connexion` use **meta `robots: {index:false}`** rather
  than `Disallow` so Google can read the noindex tag instead of leaving
  "indexed though blocked" zombies. ✅
- `generateMetadata()` present on **150 dynamic page.tsx files** (city,
  ranking, compare, red-flags, expat-retour, gentrification, calculator,
  cout-menage, vivre-avec, quitter, depuis, vacances, regions,
  departements, plus the full EN parallel tree). ✅
- New FR / EN routes wired this week:
  - `app/internet/page.tsx` → canonical `/internet` ✅
  - `app/internet/[macroregion]/page.tsx` → canonical `/internet/${slug}` ✅
  - `app/[locale]/internet-quality/page.tsx` → canonical
    `${EN_BASE}/internet-quality` ✅
  - `app/[locale]/internet-quality/[macroregion]/page.tsx` → canonical
    `${EN_BASE}/internet-quality/${slug}` ✅
  - `app/red-flags/villes-deficit-soleil-hiver/page.tsx` +
    `app/red-flags/villes-climat-2040-deteriore/page.tsx` → both pull
    metadata from `theme.metadata` and emit a `/red-flags/<slug>` canonical
    via the shared red-flag-themes pipeline. ✅
- **Canonical host correct.** Domain is `www.mavilleideale.fr` (FR) /
  `bestcitiesinfrance.com` (EN). Grep over `app/` + `lib/` for
  `localhost` / `vercel.app` / `mavilleideal.com` → 0 hits. ✅
- **Sitemap** chunks cover the new content: `/internet` + 6 macro slugs
  added to the static chunk (lines 274-280), `/internet-quality` + 6 EN
  macro slugs added to the EN static chunk (lines 798-804), plus the new
  EN `cities/[slug]/internet-quality` sub-page added to the city-sub
  rotation (line 914). The 2 new red-flag themes auto-surface via the
  `RED_FLAG_THEME_SLUGS` registry the sitemap already iterates. ✅

## 4. EN locale (`bestcitiesinfrance.com`)

Spot-checked `app/[locale]/cities/[slug]/page.tsx`: uses
`getCityTitle(city,"en")` + `getCityDescription(city,"en")` from
`lib/i18n`, not copied French strings. `descriptionEn` / `seoTitleEn` /
`seoDescriptionEn` populated on all 541 cities per CLAUDE.md (verified
2026-05-26 there; not re-grepped this run).

Static-export means the URL bar stays clean on EN (`/cities/lyon`, not
`/en/cities/lyon`) via the Worker, and `generateStaticParams` emits
`{ locale: "en" }` only.

The new `app/[locale]/internet-quality/[macroregion]/page.tsx` follows the
established EN hub pattern (`canonical: ${EN_BASE}/internet-quality/<slug>`,
no `languages` map — same as `/cycling`, `/sport`, `/employment`; hreflang
cross-linking is handled in the root layout).

## 5. Data integrity

- `data/cities-seed.ts` — **540 city slugs** (matches CLAUDE.md figure).
  Spot-checked Annecy, Nantes, Rennes: scores in `[2.8, 8.6]` range,
  region/department/lat/lng all present. ✅
- The new `topBestInternet` / `topPoorInternet` ranking helpers in
  `lib/internet-score.ts` cache the full descending list on the
  `cities` array reference, then `.filter().slice().reverse().slice()`
  per call — correct (filter copies, reverse mutates a copy not the
  cache). One micro-redundancy (`.slice()` after `.filter()`) but
  behaviour is correct.
- Vercel Analytics / Speed Insights — **not applicable**: site is on
  Cloudflare Workers Static Assets, not Vercel. Analytics is GA4 + GTM
  (GTM-MXMF7XFJ, G-4X0HKD8LC7) wired in `app/layout.tsx` head with
  Consent Mode v2 defaulting to `denied`. ✅ (Audit-spec template is
  stale on this — flagged again for future revision.)

## 6. Newsletter cron

- `wrangler.toml` `[triggers]`: `crons = ["0 7 * * SUN", "0 8 * * MON"]`.
  Sunday 07:00 UTC → `runCronNewsletter`; Monday 08:00 UTC →
  `runCronAlertes`. Dispatched in `worker/index.ts scheduled()` line
  844-847. ✅
- `.env.example` documents `BREVO_API_KEY` (REST v3, `xkeysib-` prefix),
  `BREVO_LIST_ID_FR=4`, `BREVO_LIST_ID_EN=5`. Secrets set via
  `wrangler secret put` per the `[vars]` comment in `wrangler.toml`. ✅

## 7. Navigation & mobile

- `<html>` carries `overflow-x: clip` in `globals.css` line 43, with a
  comment explaining why `overflow-x: hidden` would be wrong (would make
  `<html>` a scroll container and break `position: sticky`). ✅
- `components/Navbar.tsx` line 307 wraps in
  `sticky top-0 z-50 transition-…`. ✅
- Mobile mega-menu has `max-h-[80vh] overflow-y-auto`. ✅

## 8. Security

- `git grep -E "(api[_-]?key\s*=\s*['\"][^'\"]{20,}|BREVO_API_KEY\s*=\s*['\"])"`
  over tracked files (excluding `*.lock`, `.env*`, `*.md`) → **0 hits**.
- `git grep -E "xkeysib-[A-Za-z0-9]{20,}|sk-ant-[A-Za-z0-9]{20,}|AKIA[0-9A-Z]{16}"`
  (literal-prefix Brevo/Anthropic/AWS key shapes) → **0 hits**. ✅
- All secrets remain in Cloudflare `wrangler secret`; `.env.example`
  only contains placeholders.
- `npm install` reports **9 vulnerabilities (2 low, 3 moderate, 4 high)** in
  the transitive tree. Not opened with `--force` — auto-fix risks pulling
  a breaking next or wrangler upgrade. Worth a manual `npm audit` review
  on a non-audit session.

## 9. Bugs found this week

### Fixed (auto)

**`CLAUDE.md` lint count drift.** The `npm run lint` line said
`25 errors remaining (all react/no-unescaped-entities, cosmetic)`.
Actual is **231 errors / 27 warnings**, and the dominant cluster is
`@next/next/no-html-link-for-pages` (cosmetic under `output:"export"`),
not the unescaped-entities residue. Rewrote the comment to current numbers
+ rule shape and pointed at the audit reports for the breakdown.

### Reported, not fixed

1. **231 lint errors** — same shape as recent weeks. The
   `no-html-link-for-pages` cluster (~156) is harmless under static export
   but inflates the count; a targeted refactor of the EN locale + the FR
   forms to use `<Link>` would clean it up. Not done — scope creep, and
   risks reintroducing prefetch noise in a build that doesn't need it.
2. **6× `react-hooks/set-state-in-effect`** — `app/auth/callback/CallbackClient.tsx`,
   `app/connexion/ConnexionForm.tsx`, `app/villes/[slug]/CityProfile.tsx`,
   `components/Navbar.tsx`, plus two more. Each needs per-callsite review
   (cancellation tokens, hydration order) — not a sweep fix.
3. **9 npm-audit vulnerabilities** (2 low, 3 mod, 4 high) in the
   transitive tree. Needs a manual `npm audit` review on a session that
   can run a fresh build to verify nothing breaks.
4. **Stale audit-spec template.** The scheduled prompt still describes
   Next 14, Vercel hosting, `mavilleideal.com`, Vercel Analytics/Speed
   Insights, `vercel.json` cron config — all of which moved to Next 16 +
   Cloudflare Workers + `wrangler.toml` cron triggers + GA4/GTM. Audit ran
   successfully against actual stack but future runs would be cleaner with
   a refreshed spec. (Flagged in 2026-06-20 too.)

## Files changed

- `CLAUDE.md` — bumped stale `npm run lint` comment to current shape (231 / 27).
- `docs/audit-2026-06-27.md` — this report.
