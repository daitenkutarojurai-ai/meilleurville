# Weekly audit — 2026-06-20

Scheduled Sunday audit. Sync → tsc → SEO/metadata → EN locale → data → cron →
nav → security. Stack is Next.js 16.2.6 + Cloudflare Workers Static Assets,
not the Next 14 / Vercel template the audit spec was written against — checks
adapted to actual deployment.

## TL;DR

- **Tree clean, tsc clean.** `npx tsc --noEmit` exits 0.
- **One safe fix applied:** removed 9 dead self-referencing 301s from
  `public/_redirects` (`/comparer/<pair>` → identical `/comparer/<pair>`).
  Inverse canonical redirects (`bordeaux-vs-nantes → nantes-vs-bordeaux` etc.)
  intact; behaviour unchanged, file shrinks 109 → 100 lines.
- **No secrets leaked.** API key regex scan over tracked files = 0 hits.
- **227 lint errors / 27 warnings** — same shape as recent weeks (Next.js
  `no-html-link-for-pages` dominates, harmless under `output:"export"`).
  Listed below; not auto-fixed (cosmetic + scope creep).

## 1. Sync

`origin/main` was force-pushed since last session (50 commits replaced).
`git reset --hard origin/main` → `HEAD = 4a023ca` (`feat(pour-qui):
futurs-retraites`). No local changes lost — detached HEAD was already on a
prior tip, no in-progress work.

## 2. Build check

- `npx tsc --noEmit` → exit 0. Strict TS pass clean.
- `npm run lint` → **227 errors / 27 warnings**, breakdown:

| Rule                                       | Count | Notes |
|--------------------------------------------|-------|-------|
| `@next/next/no-html-link-for-pages`        | ~152  | EN locale pages + a couple FR forms use `<a href="/contact">` etc. Under `output:"export"` next/link gives no client-side prefetch benefit, so this is cosmetic. Migrating would touch ~20 files. |
| `react/no-unescaped-entities`              | ~69   | Pre-existing; CLAUDE.md flagged this as the long-standing residue. |
| `@typescript-eslint/no-unused-vars`        | ~19   | Mostly scripts + 2 lib files. |
| `react-hooks/set-state-in-effect`          | 6     | Hydration / token-gating patterns in `ConnexionForm`, `CityProfile`, navbar, etc. Refactoring needs careful per-callsite review — not a routine fix. |
| `react-hooks/exhaustive-deps`              | 6     | Same files, same pattern. |
| `import/no-anonymous-default-export`       | 1     | `worker/index.ts` line 727 — Worker module convention, intentional. |

None of the lint hits cause runtime bugs or break SSG. None are new regressions
visible in this week's diff.

## 3. SEO & metadata

- `app/robots.ts` — noindex strategy correct: `/auth` Disallow-ed (magic-link
  callback paths shouldn't be crawled); `/dashboard`, `/favoris`,
  `/mes-villes`, `/connexion` use **meta `robots: {index:false}`** rather than
  `Disallow` so Google can read the noindex tag instead of leaving "indexed
  though blocked" zombies. Confirmed each page emits the meta tag. ✅
- `generateMetadata()` present on every dynamic route surveyed:
  `villes/[slug]`, `classements/[slug]`, `comparer/[pair]`,
  `comparer-regions/[pair]`, `red-flags/[slug]`, `gentrification/[slug]`,
  `expat-retour/[pays]`, `calculateur-cout-reel/[ville]`, `cout-menage/[ville]`,
  `vivre-avec/[salaire]`, `quitter/[pair]`, `depuis/[slug]`. ✅
- **Canonical host correct.** Domain is `www.mavilleideale.fr` (FR) /
  `bestcitiesinfrance.com` (EN). No `localhost` or `*.vercel.app` strings in
  `app/` or `lib/`; no `mavilleideal.com` typo (the audit spec template name)
  in any source file. ✅
- **Sitemap** chunks cover all 540 cities × 40+ sub-pages, plus rankings,
  regions, departments, comparer pairs, guides, red-flags, gentrification,
  EN parallel routes. `SITEMAP_CHUNK_COUNT` re-exported into
  `app/robots.ts` so `Sitemap:` lines never drift from `generateSitemaps`.
- Layout-level `metadataBase`, `alternates.canonical: "/"`, and the FR/EN
  `hreflang` map are in `app/layout.tsx` and inherit cleanly into dynamic
  routes that don't override.

## 4. EN locale (`bestcitiesinfrance.com`)

Surveyed `app/[locale]/`. All 70+ sub-routes have either `metadata` or
`generateMetadata`. Spot-checked `cities/[slug]/page.tsx`: uses
`getCityTitle(city,"en")` + `getCityDescription(city,"en")` from `lib/i18n`,
not copied French strings. EN `descriptionEn` / `seoTitleEn` /
`seoDescriptionEn` populated on all 541 cities per CLAUDE.md (verified
2026-05-26 there; not re-grepped this run).

Static-export means the URL bar stays clean on EN (`/cities/lyon`, not
`/en/cities/lyon`) via the Worker, and `generateStaticParams` emits
`{ locale: "en" }` only.

## 5. Data integrity

- `data/cities-seed.ts` — **540 city slugs** (CLAUDE.md text says 540; figure
  agrees). Spot-checked Annecy, Nantes, Rennes: scores in `[2.8, 8.6]` range,
  region/department/lat/lng all present, character tags real. ✅
- Vercel Analytics / Speed Insights — **not applicable**: site is on
  Cloudflare Workers Static Assets, not Vercel. Analytics is GA4 + GTM,
  wired in `app/layout.tsx` head (GTM-MXMF7XFJ, G-4X0HKD8LC7) with Consent
  Mode v2 defaulting to `denied`. ✅ The audit spec template was written for
  the pre-Cloudflare era — flagging for future template revision.

## 6. Newsletter cron

- `wrangler.toml` `[triggers]`: `crons = ["0 7 * * SUN", "0 8 * * MON"]`.
  Sunday 07:00 UTC → `runCronNewsletter`; Monday 08:00 UTC → `runCronAlertes`.
  Dispatched in `worker/index.ts scheduled()`. ✅
- `.env.example` documents `BREVO_API_KEY` (REST v3, `xkeysib-` prefix),
  `BREVO_LIST_ID_FR=4`, `BREVO_LIST_ID_EN=5`. Secrets set via
  `wrangler secret put` per the [vars] comment. ✅

## 7. Navigation & mobile

- `<html>` carries `overflow-x: clip` in `globals.css` (line 43), which
  clips horizontal overflow without breaking `position: sticky` (the comment
  explicitly explains why `overflow-x: hidden` would be wrong). ✅
- `components/Navbar.tsx` line 307 wraps in `sticky top-0 z-50 transition-…`
  — sticky preserved end-to-end. ✅
- Mobile mega-menu (line 515) has `max-h-[80vh] overflow-y-auto`. ✅

## 8. Security

- `git grep -E "(api[_-]?key\s*=\s*['\"][^'\"]{20,}|BREVO_API_KEY\s*=\s*['\"])"`
  over tracked files (excluding `*.lock`, `.env*`, `*.md`) → **0 hits**.
- `git grep -E "xkeysib-[A-Za-z0-9]{20,}|sk-ant-[A-Za-z0-9]{20,}|AKIA[0-9A-Z]{16}"`
  (literal-prefix Brevo/Anthropic/AWS key shapes) → **0 hits**. ✅
- All secrets remain in Cloudflare `wrangler secret`; `.env.example` only
  contains placeholders.

## 9. Bugs found this week

### Fixed (auto)

**`public/_redirects` — 9 dead self-referencing 301s removed.**

Lines like `/comparer/nantes-vs-bordeaux  /comparer/nantes-vs-bordeaux  301`
emitted by a buggy generator that didn't skip when source==target. Cloudflare
treats these as 301-to-self (no-op at best, soft warning in some validators).
Removed:

- `/comparer/nantes-vs-bordeaux` + `/synthese` + EN `/compare/…` (×3)
- `/comparer/menton-vs-nice` + `/synthese` + EN (×3)
- `/comparer/menton-vs-antibes` + `/synthese` + EN (×3)

Inverse canonical redirects (`bordeaux-vs-nantes → nantes-vs-bordeaux` etc.)
verified intact, so non-canonical pair URLs still 301 correctly. File
109 → 100 lines.

### Reported, not fixed

1. **227 lint errors** — same shape as the last 4 audits. The
   `no-html-link-for-pages` cluster (~150) is harmless under static export
   but inflates the count; a targeted refactor of the EN locale + the FR
   forms to use `<Link>` would clean it up. Not done here — scope creep
   for a weekly audit, and risks reintroducing prefetch noise in a build
   that doesn't need it.
2. **6× `react-hooks/set-state-in-effect`** — `app/auth/callback/CallbackClient.tsx`,
   `app/connexion/ConnexionForm.tsx`, `app/villes/[slug]/CityProfile.tsx`
   review-bump effect, `components/Navbar.tsx` mega-menu close-on-route,
   plus two more. Each needs per-callsite review (cancellation tokens,
   hydration order) — not a sweep fix.
3. **Lint state diverges from CLAUDE.md.** CLAUDE.md still says
   "25 errors remaining (all react/no-unescaped-entities, cosmetic)". Actual
   is 227. Worth a doc-bump on the next content pass.
4. **Stale audit-spec template.** The scheduled prompt still describes
   Next 14, Vercel hosting, `mavilleideal.com`, Vercel Analytics/Speed
   Insights, `vercel.json` cron config — all of which moved to Next 16 +
   Cloudflare Workers + `wrangler.toml` cron triggers + GA4/GTM. The template
   should be refreshed; the audit ran successfully against actual stack but
   future runs will be cleaner with an accurate spec.

## Files changed

- `public/_redirects` — removed 9 self-redirecting lines (no behaviour change).
- `docs/audit-2026-06-20.md` — this report.
