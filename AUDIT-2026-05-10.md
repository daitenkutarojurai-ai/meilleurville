# Audit 2026-05-10 — meilleurville

Weekly autonomous audit. Run on commit `f6161ff` (post-pull `main`).

## Auto-fixed (this run)

- [32c1d02] `audit: bump fast-uri (high CVE) and other transitive deps via npm audit fix` — resolved the only HIGH severity advisory (`fast-uri` <=3.1.1, GHSA-q3j6-qgpj-74h6 path traversal + GHSA-v39h-62p7-jpjc host confusion). 7 moderate transitive vulns remain (require major bumps).
- [51342c5] `audit: bump UI/meta year labels 2025 → 2026 (titles, OG, badges)` — 14 files: city/region/department `<title>`s, classement headers, comparer badge + meta, leaderboard JSON-LD + OG, hero "Données ouvertes 2025" → 2026, FAQ wording, ranking-preview component badge.
- [5ccbde9] `audit: fix stale city count 340 → 352 in user-facing copy` — `app/villes/page.tsx` description, `app/quiz/page.tsx` title+OG, FAQ map answer, `QuizTeaser`, `lib/city-narrative.ts` per-city sentence. The seed actually contains 352 entries.

## TODO — manual attention

### Security

- **7 moderate vulns remain, all transitive of `next` 15.x or `prisma` 5.x and gated behind a major bump.** Review individually:
  - `next` (postcss range issue) — fix requires `next` major upgrade. Hold until 16.x stabilises and breaking changes can be verified.
  - `@clerk/nextjs` and `@vercel/analytics` — both flagged only because they pin older `next`. Resolve when `next` is bumped.
  - `prisma` 5 → 6 (major) closes `@hono/node-server` middleware-bypass and `hono` body-limit bypass. Per CLAUDE.md the schema is "historical" / not used at runtime — safest to either bump to 6 or just remove the dep.
  - `postcss` — fixed in next major; non-runtime risk (build-only).
- No hardcoded secrets found (`sk_live`, `pk_live`, `AKIA`, etc.).
- `.env*` is in `.gitignore`. Confirmed.

### SEO

- **No `<link rel="canonical">` set anywhere.** Recommend adding `metadata.alternates.canonical` on at least: home, `/villes`, `/villes/[slug]`, `/classements`, `/classements/[slug]`, `/regions/[region]`, `/comparer/[pair]`, `/guides/[slug]`. Otherwise Google will pick a canonical itself, and `?utm_*` / referral-tagged URLs may get indexed in parallel.
- `BASE_URL` in `app/sitemap.ts` and `app/robots.ts` defaults to `https://mavilleideale.fr` but the live deployment is `https://meilleurville.vercel.app/`. Either set `NEXT_PUBLIC_BASE_URL` on Vercel or update the default — currently the sitemap on the live URL would point cross-domain.
- `metadata.twitter.site` is `@meilleurville` (placeholder?) — confirm the handle exists.
- OG image for the `villes/[slug]/(climat|quartiers|ecoles|transports)` sub-pages: only the parent city has one, sub-pages inherit. Acceptable but a per-page OG would be better for share previews.
- 5 sampled pages all pass title length 30–60 and description length 120–160 after auto-fixes.

### Content quality

- Many guide titles/metaDesc still reference "2025" (e.g. `top-10-villes-teletravail-france-2025`). These are *guide slugs* and rotation would break URLs — leave as-is and lean on `updatedAt` field instead. New guides for 2026 already exist alongside.
- `app/cgu/page.tsx` and `app/confidentialite/page.tsx` say "Dernière mise à jour : 1er avril 2025" — legal text; if reviewed, bump the date; otherwise leave (it represents the last actual review).
- `app/dashboard/page.tsx` mock comments still dated 2025-01 / 2025-03 — fine since dashboard isn't user-facing yet.
- `data/cities-seed.ts` last touched 2026-05-09 — fresh.
- All internal `href="/<route>"` links (24 distinct) resolve to existing route files. No broken internal links.
- No `<img>` tags anywhere; no `next/image` either. Pages rely on emojis + CSS gradients + `next/og` for OG. Alt-text audit therefore N/A for static images, but `next/og` `alt` strings are present and now updated.

### Bugs

- `npx tsc --noEmit` → clean.
- `npm run build` → 2975 pages generated, no errors.
- `npm run lint` → 38 errors, 18 warnings, all from one rule: `react-hooks/set-state-in-effect`. Hot spots:
  - `components/effects/FavoriteButton.tsx:42`, `:96` — `setActive(readFavorites().includes(slug))` and `setCount(readFavorites().length)` directly in effect bodies.
  - `components/CommentSection.tsx:72`, `:78`, `:86`
  - `components/HeroSection.tsx:65`
  - `app/favoris/FavoritesGrid.tsx:33`
  - Dozens more across page files.
  - Fix pattern: read the value during render with `useSyncExternalStore` (preferred for these localStorage / event-bus reads), or guard with a `useState` initialiser. Not auto-fixed because behaviour change is non-trivial.
- No test suite present (`npm test` is not defined).

### Performance

- `.next/static` total = 2.5 MB. Within budget but the largest chunk (`0mj31swgi0dvz.js` ≈ 588 KB) likely contains `maplibre-gl` for `/carte`. Confirm it's only loaded on the map route (dynamic import) — if it leaks into the home shell, that's a regression to chase.
- Other large chunks (224 KB, 160 KB, 148 KB) likely Recharts / React internals. No action.
- `public/` contains only 5 small SVGs — no oversized images. `find public -size +500k` returned 0 hits.
- No blocking `<script>` in `<head>` — `next/font` preload + preconnect only.

### Accessibility

- Buttons across `app/villes/[slug]/CityProfile.tsx`, `app/carte/CarteClient.tsx`, `app/quiz/QuizFlow.tsx`, `app/comparer/CompareTool.tsx`, `app/red-flags/page.tsx` — all have visible text content, no aria-label needed.
- **Form inputs lack `aria-label`** in: `components/HeroSection.tsx` (search), `components/VillesSearch.tsx` (search + range slider), `components/LeaderboardTable.tsx` (search), `components/NewsletterSection.tsx` (email), `components/CostCalculator.tsx`, `components/CommentSection.tsx`, `app/quiz/QuizFlow.tsx`, `app/comparer/CompareTool.tsx`, `app/contact/ContactForm.tsx`. They have `placeholder` only — screen readers expose placeholder as a fallback, but it disappears on focus. Add `aria-label="Rechercher une ville"` etc. (10–15 minute fix; not auto-applied because each input deserves its own copy).
- `<html lang="fr">` set in `app/layout.tsx`. ✅
- Color contrast: not auditable in this environment. Run Lighthouse / axe in browser.

## Scores (1–10, 10 = perfect)

- Security: 7   (HIGH closed; 7 moderate transitive blocked behind major bumps)
- SEO: 7        (strong metadata + JSON-LD + sitemap; missing canonicals; BASE_URL mismatch)
- Content: 8    (352-city seed, 144 guides, copy now consistent on year + count)
- Stability: 8  (TS + build clean; 38 lint errors all from one new React-19 rule)
- Performance: 8 (2.5 MB static; verify maplibre-gl scope)
- Accessibility: 7 (semantic HTML strong; form-input labels missing)
- **Overall: 7.5**

## Trend vs last week

No prior `AUDIT-*.md` in repo — this is the first weekly ultra-audit.
