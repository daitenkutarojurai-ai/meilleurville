# Cloudflare Workers Static Assets redirects.
# Replaces the next.config redirects() that static export cannot emit.
# NB: _redirects only matches paths — cross-host (apex->www) redirects are NOT
# supported here, so host canonicalization is handled in worker/index.ts.
/a-propos /about 301
